[build]
  base = "."
  command = "echo Static deploy from repo root"
  publish = "."

[build.environment]
  NODE_VERSION = "18"
  CI = "false"

[[redirects]]
  from = "/"
  to = "/enterprise-ui.html"
  status = 200
